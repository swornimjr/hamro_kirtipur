{
  "name": "kirtipur-directory",
  "version": "1.0.0",
  "description": "Kirtipur local directory — searchable map of services, wards, and emergency contacts",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
